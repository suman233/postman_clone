//   const addParamsToUrl = () => {
//     if (!url || !paramsArr.length) {
//       setFinalUrl(url);
//     } else {
//       setFinalUrl(`${url}${mapParamsToString()}`);
//     }
//   };
//   const resetUrl = () => setFinalUrl(url);
//   return [finalUrl, addParamsToUrl, resetUrl] as const;
// };
// export default useAddParams;